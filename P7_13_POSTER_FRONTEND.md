# P7.13｜海報輔助開房前端

版本 v14.2.26 / build 20260925.37。延伸原 AI 協助建立；不另建房間流程。

## 房主入口
賽事設定 → AI 協助建立 BETA → 選擇 JPG／PNG 海報 → 選填補充文案 → 開始解析 → 核對辨識結果與海報文字 → 套用選定賽事至草稿 → 選擇是否套用 BXH 賽事公版 → 房主審核 → 原有儲存／發布。

## 契約與限制
- 呼叫獨立 parseTournamentPosterV1；contractVersion = bxh-arena.ai-poster.callable.v1。
- 正式 bxh-arena / asia-east1；沿用 engagementService 的登入狀態與工作階段檢查。
- JPG／PNG 原圖 12 MB 以內；本機縮圖到長邊 2048px、傳輸 1.5 MiB 以內。圖片不設為活動封面、不進 localStorage 或房間 state。
- 選圖只在本機處理；按開始解析才送圖片和補充文字。HEIC／PDF／GIF 不支援。
- 維持 super_admin／白名單 admin；inactive、test、frozen、deleted 帳號不套用；伺服器仍是最終權限權威。
- 海報解析失敗不偷偷退回文字解析。取消／換圖／修改文案／切換帳號或房間使舊回應失效。
- 套用海報時，預覽中的日期、時間、場地、名額、費用更新至草稿；未辨識欄位留空，不沿用上張海報值。格式未明確仍沿用原有人工確認機制。
- 已有手動活動說明不覆蓋；空白說明才帶入海報轉錄與補充文字。獎品只保留可讀文字，不從包裝圖案猜名稱。
- 公版勾選與公版快照不修改；AI 不呼叫公版儲存、建房、Firestore 寫入或發布。
- 圖文衝突不透過主活動欄位補回選定賽事的衝突值；所有海報警告保留在套用後 Review Gate，房主確認後才可繼續。

## 驗證
- 41 項 Node 回歸測試：契約、型別、日期、來源過期、重複解析、權限、草稿隔離、公版、手動說明、衝突、審核警告與 escaping。
- Chromium 離線介面驗證：390×844 與 1280×900；3000×4000 合成 PNG 經真實瀏覽器縮圖至 1536×2048；圖片單獨解析及移除流程通過，無 JavaScript error。此處解析服務使用 mock，不是實際 AI 準確率測試。
- 尚未驗證使用者實際海報、正式帳號的端到端辨識、iPhone／Android 真機；不能把上述測試視為完成這些驗收。

本次不改 Firebase Functions、Rules、會員資料、天梯、抽獎扣券或比賽計分。
